/**
 * INPUT: Future session records, turns, trace events, and persistence configuration.
 * OUTPUT: Session package boundary for future conversation and trace storage.
 * POS: Session storage layer placeholder; will persist replayable agent state.
 *
 * Update this header and the parent directory docs when responsibilities change.
 */
export const sessionsPackageName = "@arvinclaw/sessions";
