/**
 * INPUT: Future local SKILL.md files, skill metadata, and workspace skill paths.
 * OUTPUT: Skill package boundary for future discovery and prompt integration.
 * POS: Skill system layer placeholder; will expose compact skill context to the agent.
 *
 * Update this header and the parent directory docs when responsibilities change.
 */
export const skillsPackageName = "@arvinclaw/skills";
