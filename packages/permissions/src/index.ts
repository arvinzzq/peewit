/**
 * INPUT: Future tool actions, autonomy mode, and permission context.
 * OUTPUT: Permission package boundary for future risk classification and approval policy.
 * POS: Permission layer placeholder; will decide allow, ask, deny, or block outcomes.
 *
 * Update this header and the parent directory docs when responsibilities change.
 */
export const permissionsPackageName = "@arvinclaw/permissions";
