/**
 * INPUT: Future tool definitions, tool inputs, and execution context.
 * OUTPUT: Tool package boundary for future registry, validation, and execution wrappers.
 * POS: Tool system layer placeholder; will expose capabilities without making permission decisions.
 *
 * Update this header and the parent directory docs when responsibilities change.
 */
export const toolsPackageName = "@arvinclaw/tools";
